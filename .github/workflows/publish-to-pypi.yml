name: Publish to PyPI

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  id-token: write

jobs:
  build-and-publish:
    name: Build and publish to PyPI
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install uv
      uses: astral-sh/setup-uv@v2
      with:
        enable-cache: true

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install system dependencies
      run: sudo apt-get update && sudo apt-get install -y portaudio19-dev

    - name: Run tests
      run: uv run --all-extras pytest
    
    - name: Build package
      run: uv build --all-packages

    - name: Publish aiovban to PyPI
      run: uv publish --check-url https://pypi.org/simple/aiovban/ dist/aiovban-*

    - name: Publish aiovban-pyaudio to PyPI
      run: uv publish --check-url https://pypi.org/simple/aiovban-pyaudio dist/aiovban_pyaudio-*

    - name: Extract version from tag
      id: get_version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        name: Release v${{ steps.get_version.outputs.VERSION }}
        files: |
          dist/*.whl
          dist/*.tar.gz
        draft: false
        prerelease: false
        generate_release_notes: true
